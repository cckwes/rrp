export enum DrawCardActionType {
  INITIALIZE = "initialize",
  SELECT = "select",
  UNSELECT = "unselect",
  START_DRAWING = "start-drawing",
  DRAW_RESULT = "draw-result",
  RESTART_DRAW = "restart-draw",
}
